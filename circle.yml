checkout:
  post:
    - yarn global add gulp

machine:
  node:
    version: 7.10.0
  services:
    docker

dependencies:
  override:
    - yarn
  cache_directories:
    - ~/.cache/yarn

test:
  pre:
    - docker-compose up --build -d
  override:
    - yarn test
