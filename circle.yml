checkout:
  post:
    - yarn global add gulp

machine:
  node:
    version: 7.10.0
  services:
    docker

dependencies:
  override:
    - yarn
  cache_directories:
    - ~/.cache/yarn

test:
  pre:
    - docker -v
    - docker-compose -v
    - docker-compose up -d
  override:
    - yarn test
